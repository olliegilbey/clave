/target
# cargo-mutants scratch output (`just mutants`); the findings go in the PR
# dossier, never in the tree.
/mutants.out
/mutants.out.old
.DS_Store
*.swp
.claude/settings.local.json
# subagent-driven-development scratch: task briefs, reports, progress ledger
.superpowers/

# Agent worktrees live here; they are ephemeral checkouts, never repo content.
.claude/worktrees/
