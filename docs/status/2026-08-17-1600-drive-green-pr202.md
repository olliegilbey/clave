# Status — QA drive FULL GREEN (run 4, 0-7 + both eyeballs); PR #202 open, riding review

_Follows @docs/status/2026-08-17-1447-qa-drive-e2e.md (the phase-3 wedge
handoff). Worktree `qa-182-drive-slice-1`, branch `qa-drive-e2e`, pushed;
PR #202 open against main. Tree clean except this file and
`scripts/tmp-review-poll.sh` (throwaway, delete before merge)._

## Task Overview

Ollie's original instruction (verbatim, still governing):

> Then we need to do the full end to end qa drive as we've started to spec it,
> to check the full system for release, everything. By you driving and
> testing, and anything I need to do additionally to check it. Then need to
> see about switching my main clave to it, testing it for a little bit, and
> then considering the release if all is good.

## Current State — the drive part is DONE

- **Run 4 (2026-08-17 ~15:35): full 0-7 PASS.** Both eyeball checkpoints
  confirmed by Ollie ("things seem very good and consistent. The best I've
  seen so far."). Collapse burst: 18 presses, exactly 1 store write each,
  parity held. Quiescence: 60s dead flat.
- Runs 1-3 each went red on exactly one real finding, all fixed on this
  branch: run 1 the stale-executor nav wedge (`c585ff3`), run 2 the
  starved-bar prune of a newborn bind (`fd33114`), run 3 the script's
  `jq // empty` blindness to `false` (`3bd173f`). All three have FOOTGUNS
  entries; the two product fixes were TDD'd and mutant-clean (39/0 and
  14/0 missed).
- **PR #202** carries all of it + drive phases 5-7 (`f150dc5`) + the docs
  ledger commit. CI fully green (one wasm-build red was a GitHub 429
  fetching the cache action; rerun passed). NO review activity yet —
  a 30-min poll was running at handoff (`scripts/tmp-review-poll.sh`).
- Sandbox: Ollie has killed/deleted it; nothing left to read there. Drive
  logs live under `~/.local/state/clave-dev-qa-182-d-1d57/state/qa/`.

## Next Steps

1. Ride PR #202 review: CodeRabbit/Codex rounds — fix every finding, reply
   per thread before resolving, never silent-resolve. Delete
   `scripts/tmp-review-poll.sh` in the next push (it is uncommitted; just
   don't commit it).
2. Merge on green + approval. Then the daily-driver switch is OLLIE'S:
   his commands write `~/.cargo/bin` / `~/.local/share/clave` — print,
   don't run. Dogfood window on his real fleet, then his v0.1.3 tag +
   `just release`.
3. File follow-up issues (not release blockers unless Ollie says so):
   - **Launch/resume hydration gap (Ollie's eyeball finding, run 4):** a
     resumed/launched row shows only the seeded label until the first hook
     write, though the transcript jsonl already holds the ai-title, token
     count, and worktree provenance. `clave` should hydrate these at
     launch/resume from the jsonl. Also: first tab ran in a worktree with
     no worktree provenance glyph in the bar.
   - **Drive gap:** the drive should corroborate bar rows against
     transcript jsonl content (title/tokens/provenance), not just the store.
   - **Evlog interleaving:** concurrent appenders produced one mangled
     line (two writes interleaved) in the sandbox `clave.log` — benign but
     real; seen run 3 ~15:19:19.

## Context to Preserve

- A failed phase is evidence, not an excuse to re-run until green.
- Background every CI/review wait; report between waits.
- Report register: outcome first, no unglossed symbols, six-ish sentences.
- Restage loop: Ollie kills (kill-session + delete-session --force
  clave-test-qa-182-d-1d57), agent runs `just sandbox qa-fleet`, Ollie
  launches the printed line, agent runs `scripts/qa-drive.sh qa-fleet`.
- #65 stays ignored per Ollie.

## Restart Hint

If review landed: work the threads on PR #202. If merged: hand Ollie the
daily-driver switch steps and stop — the tag and `just release` are his.
