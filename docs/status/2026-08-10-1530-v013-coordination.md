# Status — v0.1.3 coordination: board nearly clear, #148 is the long pole

_2026-08-10 15:30 · CLV-MAIN coordinating session handing off before /clear.
Everything this session touched is MERGED AND CLEANED UP; what remains is the
back half of the milestone and three flagged loose ends._

## Task Overview

Act as the coordinating agent for **v0.1.3 (fleet legibility, map issue #115)**:
land review rounds, drive live validation, keep the board/worktrees clean, and
sequence the remaining tickets to the release cut. Success = every v0.1.3
child ticket closed, then the cut (`just release` — HIS, never yours).

## Current State

Merged to main this session (all squash + branch-delete, worktrees removed,
per-agent sandboxes reaped): **#161** (per-agent sandbox roots + reaper —
verified per its own handoff checklist, two Codex P1 fixes in `69a7e8b`),
**#151/#112** (dormant segregation, merged `732f8b6`, live-validated), plus
earlier in the thread #143/#139 and #150/#149. Merged by other sessions
meanwhile: #152/#137 (width-storm + `ct.sh` wrapper), #147/#62 (battery).
Local main = `732f8b6`, synced. Filed **#162** (see Discoveries). Map #115 has
a Decisions-so-far comment per closed ticket (#112's posted this session).

Uncommitted: only `CLAUDE.md` in the main checkout (Ollie's own edit, 2 lines
— his tree on `s7-context-battery`, NEVER touch it).

## What's Working

- **Per-agent sandboxes are live and field-proven.** From any worktree:
  `just sandbox [scenario]` (ALWAYS env-stripped:
  `env -u ZELLIJ -u ZELLIJ_SESSION_NAME -u ZELLIJ_PANE_ID just sandbox`),
  ask `clave dev instance` for this tree's session/root, `clave dev reap`
  cleans orphans. Two sandboxes coexisted under drive with zero cross-writes.
- **Ollie delegates sandbox validation END-TO-END** (his words, 2026-08-10):
  agent stages and drives, he launches/kills and eyeballs. Gate every drive
  command fail-closed: socket exists at
  `${TMPDIR%/}/zellij-501/contract_version_1/<session>` AND
  `pgrep -f "zellij --server <socket>"` alive — `${TMPDIR%/}` is load-bearing
  (macOS trailing slash breaks pgrep). `scripts/ct.sh` on main does this for
  the main sandbox only; the per-worktree rework is UNPUSHED (see Next Steps).
- **Keybind emulation for drives**: the bar's whole control surface rides one
  pipe, `clave-nav`, payloads `{"dir":"next"|"prev"}`, `{"row":N}`,
  `{"commit":true}` — send via
  `zellij --session <s> action pipe --name clave-nav -- '<json>'`. Works ONLY
  while a launch-time bar instance is alive (#162). Never pipe with
  `--plugin` unless `--plugin-configuration clave_binary=clave` matches, or a
  second bar spawns.
- **The review loop is mechanical**: verify claim against code → fix or
  decline-with-reason → reply citing the sha → THEN resolve (GraphQL
  `resolveReviewThread`). `grep -c "/Users/"` any body before posting (repo
  public; grep exits 1 on zero matches, don't chain with `&&`).
- **Drive-loop snapshots must be SORTED**: `find … -exec shasum {} + | sort` —
  unsorted traversal order flips between runs and fakes a diff (cost a round
  this session).

## Important Discoveries

- **#162 (filed this session): broadcast pipe payloads reach only the
  earliest-born bar instances.** When every launch-time bar dies with its tab,
  ALL surviving instances (including freshly created ones) receive every pipe
  payload-less, permanently; the executor election rides the `clave-visited`
  beacon, so the whole session's nav goes dark — only Alt+c (payload-less,
  instance-local) works. Reads as "sidebar is broken". Repro + two mitigation
  sketches in the issue; FOOTGUNS entry landed with #151. NOT #112's fault —
  its drive validated green apart from the zero-live wake, which is blocked
  by #162, not failed.
- **#112's zero-live wake is the one unexercised item** — needs #162 fixed or
  a bar-only cold start to test honestly. Recorded on the map comment.
- Squash-merged branches: `git branch -d` refuses; verify
  `gh pr view <n> --json headRefOid` equals the branch tip, then `-D`.
  Three-dot diffs across a squash re-show already-merged content — compare
  trees two-dot (`git diff main <sha>`).
- Commits carry `Claude-Session:` trailers now; cloud (claude.ai/code)
  sessions leave no local transcript — that's where "can't find the agent"
  answers live.
- Two zombie `zellij --server` processes hold the LIVE session's socket path
  (one dated 31 Jul) — flagged to Ollie as a #154 clue; untouched.

## Next Steps

1. **#148 (viewport)** — release-blocking, unassigned, THE long pole. Its
   design partner #112 is merged, so it's ready: claim it (assign
   olliegilbey), brainstorm before implementation per map Notes, one ticket
   per session.
2. **#114** — blocked ONLY on Ollie's vertical-budget call; carries the
   config-passthrough build absorbed from #122. Nudge him for the decision.
3. **Seek-storm worktree holds UNPUSHED work**: `.claude/worktrees/
   seek-storm-137` at `663b5c5` (local branch `fix/137-width-seek-storm`, no
   remote) — a ~32-line `ct.sh` per-worktree rework + seek scripts + spec
   edits, apparently answering this session's two #152 comments. Another
   agent may own it; check before adopting, then push + PR.
4. **Triage with Ollie**: does #162 join v0.1.3 (fleet-legibility theme says
   maybe)? Remaining build tickets #146, #105, #92, #57.
5. **Doc-branch strays**: `research/zellij-config-passthrough` (365-line doc,
   should ride #114) and `docs/c1-config-passthrough-ledger` (~60 C-section
   lines differing from what #138 merged — diff, fold, delete).
6. Then the release cut: changelog, tag, `just release` (his), container
   check #76, runbook #35/#49.

Where work stopped — Ollie, verbatim:

> can you clean up the local, pruning, killing dead worktrees, etc.
> Then I need you to do a /handoff for yourself to be able to pick up what
> needs after clearing context and coming back strong as the coordinating
> agent.

Endorsements worth carrying — on the sandbox drive and the merge run:

> now we have the sandbox separation so that you can drive it instead of me
> end to end, and I can come in and check the last pieces.

> Okay, let's get 151 in. Were you werking on that? Everything else is in now.

## Context to Preserve

- **His session — nothing, not even a read. Session lifecycle — his, always.
  Never `just release`, never write `~/.local/share/clave/`, `git add`
  explicit paths only.** His main checkout rides `s7-context-battery`
  (merged branch; switching back to main is HIS call — flag, don't do).
- Commits: wait for his 1Password signing; author must be him
  (`--reset-author` if an agent identity slips in).
- CodeRabbit "pass / Review rate limited" = reviewed nothing. Expect
  multi-round reviews; never silent-resolve (some #143 threads were
  silent-resolved in the UI — replies were backfilled this session).
- Map #115 body is last-write-wins: COMMENT, never edit.
- zsh traps: `echo ===x===` breaks; `${TMPDIR%/}` everywhere.
- Worktrees standing: `claude-codex-profile` (parked draft #65, 3 dirty
  files, HIS), `seek-storm-137` (unpushed work, see Next Steps).

## Restart Hint

Everything merged and pushed; only Ollie's own CLAUDE.md edit is uncommitted —
safe to /clear. Start at `/catch-up`, then claim #148.
