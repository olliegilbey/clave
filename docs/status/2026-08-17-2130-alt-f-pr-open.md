# Status — #207 shipped to PR #209; drive green, review round one in flight

Supersedes @docs/status/2026-08-17-1943-alt-f-toggle-207.md, whose Next Steps
are DONE — that file described the then-uncommitted fix and is kept as the
dated record of the second-bar regression forensics (Codex P2, PR #209, asked
for exactly this superseding note).

## Current State

Branch `worktree-alt-f-toggle-207`, PR #209 open, CI green (lint, test,
wasm-build, plan), CodeRabbit full review + Codex commented.

Done since the prior handoff:

- Fix committed (`a9fecee`), FOOTGUNS second-bar entry (`0f219d1`), geometry
  trim y=4/w=98/h=92 after Ollie's live eyeball (`243577f`, "spot on").
- Clean-session drive fully green, all phases: baseline, spawn (x=54, 97%→98%
  width), hide-persists, show, 5 rapid presses no stacking, close-then-respawn,
  60s quiescence. Keybind path + width confirmed by Ollie by hand.
- Alt+c-under-floating cosmetic bug filed as #208 (low priority, mechanism
  hypothesis in the issue).
- Review round one: spawn-latch (`shell_spawn_pending`) added for CodeRabbit's
  key-repeat double-spawn Major, with test; FOOTGUNS Alt+f wording corrected.
  Codex P1 (any floating pane counts as the shell) answered as intended
  layer-semantics — zellij hide/show are layer-wide, so per-pane tracking buys
  nothing; flagged to Ollie as a judgment call.

## Next Steps

1. Finish the review loop: reply "Ollie's Agent Speaking:" + resolve fixed
   threads, expect further CodeRabbit rounds, merge when green and Ollie says.
2. Then the v0.1.3 tag decision (retag vs v0.1.4 — tag cut locally, not
   pushed) and the queue in
   @docs/status/2026-08-17-1804-v013-dogfood-window.md Next Steps 2-5:
   reference screenshot, branch cleanup, #206 (dimming).
3. Sandbox `clave-test-alt-f-to-3c9c` is live and clean; kill is Ollie's when
   done.
