# Status — PR #183 open with a GREEN verdict; review round 1 fixed+pushed; cycle-5 drive is next and the sandbox is LIVE

_Supersedes 2026-08-12-1731-v013-fix-and-qa.md (committed on this branch;
read it only for the pre-PR build history). Working directory:
`.claude/worktrees/qa-182-drive-slice-1`, branch
`worktree-qa-182-drive-slice-1`, clean tree at `33e27fc`, pushed._

## Task Overview

v0.1.3 is HELD on #178. PR 1 = the QA harness (#182, THIS branch, now
**PR #183**) — phases 0–2 of `scripts/qa-drive.sh`. The evidence it
carries flipped from the expected red to **GREEN**: cycle 4 ran all
green (attempted=5 landed=5), so the branch's verdict is "a fresh 6-tab
qa-fleet sandbox on this build does NOT reproduce #178" (posted to #178
as an evidence comment). PR 2 will instrument the bind budget and hunt
the aggravator that turns the drive red. Merge is Ollie's; then he
re-tags v0.1.3.

## Current State

- PR #183 open, 4 commits `c23febc..33e27fc` on top of the reviewed
  base. `just gates` + shellcheck green at HEAD.
- CodeRabbit round 1: 13 inline threads. 11 FIXED in `33e27fc`, 2
  REJECTED with source evidence. **Replies + resolutions NOT yet posted
  — that is the debt this handoff carries** (protocol: fix → reply
  "Ollie's Agent Speaking:" → resolve; never silent-resolve).
- Cycle-5 drive NOT yet run. It re-validates what `33e27fc` changed:
  add.rs `--session` threading (create rung), the new eager
  resume-identity check, the wakeable-rank targeting. Sandbox is
  **staged AND launched by Ollie — LIVE right now**, fresh 6-row store
  on the `33e27fc` release build (session `clave-test-qa-182-d-1d57`).
- SDD ledger `.superpowers/sdd/2026-08-12-qa-drive-slice-1/progress.md`
  holds every fix round (2–4 pre-PR, round-1 review entry last). Trust
  it over memory; never commit `.superpowers/`.

## What's Working

- **The drive is proven green end-to-end** (cycle 4, log
  `state/qa/drive-1786628188.log` in the sandbox state dir): eager +
  create + 4 wakes all bound ~2s, ghost stranded by design, wakes in
  exact ordinal order. Build on the harness as-is; only re-run, don't
  redesign.
- **The four-cycle loop works**: Ollie kills+launches, agent restages
  (`just sandbox qa-fleet`) and drives. Each red cycle so far was a
  HARNESS bug, found from the sandbox's own `clave.log`/store, fixed
  same-session. Drive log paths print at run start.
- **The rejected-findings pushback is verified**: B13 floor=32 is
  asserted at `crates/clave-bar/src/render.rs:1467-1471` (29 is
  pre-#105); bar-render B14's anchors `model.rs:392`/`:703` are exact
  (`birth_touched`, `needs_birth_touch`). Quote these in the replies.
- add.rs `--session` threading is gate-green and safe in the field:
  `env::session_name()` = `$CLAVE_SESSION` or the dedicated "clave"
  session, so the field path names the session it's already inside;
  a dead named session now exits 1 (fail-closed) instead of leaking
  onto the sole live session.

## Important Discoveries (this session's, all FOOTGUNS'd/committed)

- The zellij log is user-global; its 25-char source column collapses
  stable+sandbox wasm paths (usernames ≥9 chars) — twin deltas are
  forensics, never assertions. The bar's panes are INVISIBLE to
  `list-panes` (count instances as live tabs). Dormant block renders
  ordinal-DESC, ghost oldest → bottom; wake targeting must rank to the
  first WAKEABLE row. A minted row is store-dormant until its bind
  lands (sub-second race). `zellij --session <n>` fails closed; env
  vars fail open.
- GREEN evidence constrains #178: the trigger needs an aggravator a
  fresh 6-tab sandbox lacks (scale/age/churn/collapse) — see the #178
  comment posted 2026-08-13.

## Next Steps

1. **Run cycle 5 NOW — the sandbox is live and waiting**:
   `scripts/qa-drive.sh qa-fleet` from the worktree. Expect all green
   incl. the new "eager resume targets live_session" check and
   rank-walked wakes. On green: request Ollie's two eyeballs
   (bars-per-tab, widths), then hand him the kill pair
   (`zellij kill-session clave-test-qa-182-d-1d57 ; zellij
   delete-session --force clave-test-qa-182-d-1d57`). On red: forensics
   from sandbox clave.log + store BEFORE teardown; grep FOOTGUNS first.
2. **Reply to all 13 threads + resolve** (ids in
   `gh api repos/olliegilbey/clave/pulls/183/comments`). 11× "fixed in
   33e27fc" (one line each, cite the cycle-5 log line where relevant);
   2× rejection with the source quotes above. Prefix every reply
   "Ollie's Agent Speaking:". Resolve via GraphQL
   (resolveReviewThread) after replying. Expect a round 2.
3. Commit this status file + the cycle-5 ledger entry with whatever
   comes next; then the PR waits on Ollie's merge → PR 2 (#178
   instrument-first, see SUBSYSTEM-VALIDATION S0/C5 + FOOTGUNS
   TabUpdate-starvation) → re-tag.
4. Parked: the created tab renders slightly narrower (Ollie: "Separate
   issue probably. And only minor." — in ledger, unfiled). #180, #181,
   phases 3–6, cross-session-bleed sighting (lead: shared zellij
   cache/permissions.kdl; now ALSO note the shared zellij.log) all
   queue post-cut.

Stopping exchange, verbatim (Ollie, /handoff args): "and the session is
up for you to drive when we come back from the handoff doc". Cycle-4
eyeball, verbatim: "all tabs are agents. And nav of my main clave
session doesn't seem to be affecting the sandbox session at the same
time right now, which is good."

## Context to Preserve

- **His zellij session: nothing, not even a read**; sandbox only via
  ct.sh/qa-drive.sh; launches/kills/`just release`/`~/.local/share/
  clave` writes are HIS. Merges his. Commits Ollie-signed narrative
  with Claude-Session trailer; `grep -c "/Users/"` public bodies;
  "Ollie's Agent Speaking:" on anything humans read as him.
- The stray "1" in drive logs during `clave add` is zellij CLI chatter,
  labelled in the log — not a bug. Twin-delta measures reading 10–34
  are main-fleet pollution — expected, not red.

## Restart Hint

Tree clean and pushed; sandbox LIVE — resume at Next Step 1 immediately
(drive first, replies second). /catch-up from the worktree.
