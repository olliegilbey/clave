# Status — v0.1.3: all build tickets merged + live-validated; finish the release tail

_2026-08-11 ~15:10 · principal/coordinating session checkpointing near context
limit. This file supersedes @docs/status/2026-08-10-1930-v013-coordination.md
and @docs/status/2026-08-10-1530-v013-coordination.md (both fully resolved).
Written to be sufficient with no prior conversation — trust the repo and
GitHub over this file where they disagree._

## Task Overview

Ship **v0.1.3 (fleet legibility, wayfinder map issue #115)**. All five build
tickets are MERGED and LIVE-VALIDATED. What remains is the tail: one docs PR,
the map close, and the cut itself. Issue and PR numbers overlap confusingly —
always spell pairs (issue ← PR).

## Current State (verified 15:05)

- main at `e646049` (may have moved — `git pull` first). Merged this cycle,
  in order: docs #169 · #109 ← PR #168 · #148 ← PR #170 · #105 ← PR #172 ·
  #162 ← PR #171.
- **PR #176 (docs: drive lessons + glossary softening) is OPEN**: branch
  `docs/v013-drive-lessons`, worktree `.claude/worktrees/docs-tail`, commit
  `1e2c0fd` (Ollie-signed). CI green but merge BLOCKED: **CodeRabbit left FOUR
  unresolved threads** (unread — the session ended before triaging). First
  real action next session: read all four (GraphQL reviewThreads on PR 176),
  fix what's valid / rebut what isn't, reply to each, resolve each, then
  `sleep 90; gh pr checks 176 --watch && gh pr merge 176 --squash`. After
  merge: `git worktree remove .claude/worktrees/docs-tail; git branch -D
  docs/v013-drive-lessons`, pull.
- Live validation (2026-08-11, build e646049, Ollie driving real keybinds,
  34-row `tall` scenario in a 30-line pane): viewport follow/lookahead/wrap/
  snap-home/clicks all "work perfectly"; both #162 tier-3 scenarios pass
  (new-tab + one Alt+j = exactly one change; close-last-agent-tab + Alt+j
  still answers — dead pre-fix); battery 26k→37k matching the real figure,
  one-turn lag is documented behaviour. Validation comments are ON the
  tickets (#148, #162, #105). Sandbox torn down by Ollie.
- Session task board and SDD ledger
  (`.superpowers/sdd/2026-08-10-v013-cut/progress.md`) hold the full trail;
  the ledger + task-N-report.md files can be deleted once the map closes.

## Remaining, in order

1. **Land PR #176** (see above).
2. **Changelog**: no CHANGELOG file exists anywhere (verified; tags
   v0.1.0-v0.1.2 shipped without one). Read `docs/dev/RELEASE-RUNBOOK.md`
   end-to-end for what a cut ACTUALLY requires before inventing anything.
3. **Close the map**: comment on #115 (COMMENT, never edit the body —
   last-write-wins) summarising the cut: five tickets with PR pairs, live
   validation done, follow-ups filed (#167 dist-build tag, #173 tall-scenario
   token seeding, #174 Alt+Enter affordance, #175 Alt+1..9 in overflow;
   punted: #92, #146 (has fresh pane-title-lag evidence), #57, #114). Close
   #115. Close the v0.1.3 milestone (`gh api -X PATCH` or leave to Ollie).
4. **Print for Ollie, never run**: the tag push and `just release`. Remind
   him runbook Part C step 3 now demands `build=` equal the EXACT cut tag on
   the released bar (a `dev` or short-SHA there is a STOP — #109 ← PR #168).
5. Post-cut items belong to later sessions: container check #76, runbook
   #35/#49.

## Hard constraints (standing, non-negotiable)

- **His zellij session: nothing, not even a read.** Session lifecycle,
  `just release`, `~/.local/share/clave/**`, `~/.cargo/bin/clave`,
  `~/.config/zellij/**` — his. Sandbox driving only via `scripts/ct.sh`
  (fails closed); the env-var route fails OPEN onto his live fleet.
- Commits: author + signature = Ollie (1Password; times out when he's away —
  if a commit lands Claude-signed, re-sign via
  `git rebase --force-rebase origin/main` in the branch worktree before PR).
- `grep -c "/Users/"` any body before posting to GitHub (public repo).
- Comment-attribute as "Ollie's Agent Speaking:" on anything humans read as
  him.

## Traps that cost rounds THIS cycle (all now also in FOOTGUNS via #176)

- **CodeRabbit protocol**: "pass / Review rate limited" = reviewed NOTHING.
  The CLI lane works meanwhile: `coderabbit review --committed --base main
  --agent` from the branch worktree. Branch protection has
  `required_conversation_resolution=true` — an unresolved thread blocks merge
  with the misleading "base branch policy prohibits"; every thread must be
  fixed + replied + resolved, never silently.
- **Merge chains**: `sleep 90` between push/update-branch and
  `gh pr checks --watch` (checks register late; watching early races the
  merge onto a partial set). Merging PR N forces update-branch + fresh CI on
  PR N+1 (strict up-to-date). Run merges from the repo root, not inside the
  PR's worktree.
- **Drives**: dir-nav wraps WITHIN one block — from a single live tab it
  bounces in place; pick into the dormant block (`{"row":N}`) before walking.
  Never `>/dev/null` a drive loop. Ollie prefers driving the keybinds
  himself, agent corroborates in the zellij log afterwards.

## Loose ends (cheap, optional)

- One stale REMOTE branch: `s7-context-battery`, fully merged (the
  2026-08-10 handoff claimed it deleted; the remote half never happened).
  Delete is Ollie's: `git push origin --delete s7-context-battery`.
  (`feat/105-token-battery` turned out already gone — a multi-ref delete
  aborts wholesale if any ref is missing; delete refs one at a time.)
- This handoff itself is UNTRACKED in the main checkout on purpose (so
  /catch-up finds it); commit it with the map-close docs, same as the
  previous handoffs rode #169.
- `/tmp/zellij-0.44.3` clone (read-only source) can be deleted.
- Memory note exists: Ollie flagged plans/docs as overengineered — KISS bias,
  descope openly (memory file kiss-over-engineered-plans).

## Restart Hint

`/catch-up` lands here. First actions: `git pull`, `gh pr view 176`, then
walk "Remaining, in order". The board work is done — this is a closing
ceremony, not a build session. Keep it short and ship the cut.
