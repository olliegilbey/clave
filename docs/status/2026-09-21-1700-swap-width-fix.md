# Task Pickup

You are picking up from the agent that turned the 0.2s collapse lag into a
one-swap switch, ran both reviews, and opened the PR from `diag/swap-width`.

## Orientation

- The fix: `Effect::SwapWidth { backwards }`. Away from the birth width the bar calls `previous_swap_layout`, towards it `next_swap_layout`. Birth = first paint at a declared width, recorded before the hydration gate | **Checked** | `crates/clave-bar/src/model.rs` `width_effects`; commit `3e980b4`
- Reviews: coderabbit found nothing. A blind Opus review verified the direction rule against zellij's `swap_layouts.rs` (backward from birth wraps to declared[1]), walked every wrong-birth case (worst: the old two-step, no loop), and found the host-call mapping untestable. Fixed in `23ee229` with a source-text test in `lib.rs` that fails when previous and next are swapped | **Checked** | `cargo test -p clave-bar --lib the_swap_direction`
- Declined: unknown birth on a damaged tab can flip direction mid-walk and cost a third ask. Needs first paints at neither declared width with birth still in the ring; the layout construction makes that near impossible | **Checked** | the PR body, "Findings DECLINED"
- The XPS is still on `pane_frames false`. The human wants frames on for both machines | **Open** | `ssh devbox 'sed -n 333p ~/.config/zellij/config.kdl'`
- After the human interrupts a Claude turn, the row keeps the turn clock and spinner. Not started | **Open** | interrupt a turn in a sandbox and read the store's status

## Current State

- Branch `diag/swap-width` pushed, PR open, awaiting the human's merge.
- Sandbox `clave-test-swap-diag` may still be running; kill by name only.
- Recommendation given: merge, cut the next release, install on the devbox with `clave setup`. Rollback is a reinstall of v0.5.1.

## Next Steps

1. After merge: `just release` on the next tag, install on both machines.
2. XPS frames: `pane_frames true` and `pane_frame_style "full"` on devbox, then `clave setup` over ssh, then his relaunch.
3. The previous handoff's open docs items (`docs/status/2026-09-18-0858-xps-collapse-flap.md`, Next Steps 5-6) and the `term-chip-green` worktree.
4. The interrupted-turn spinner defect.
