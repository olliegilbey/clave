# Status — v0.1.3 dogfood found the release blocker: Alt+f must toggle (#207)

You are the release closer for clave v0.1.3: the QA drive is green, PR #202
is merged, the binary is installed and Ollie is daily-driving it — and the
dogfood window did its job by finding exactly one blocker. Your first task
is fixing #207 test-first; after that you sweep the short release-hygiene
list clean, hand Ollie the tag push, and start the dimming feature (#206).
Work in his register: outcome first, no unglossed symbols, ~six sentences,
decision over mechanism, KISS always.

_Follows @docs/status/2026-08-17-1600-drive-green-pr202.md (drive-green
handoff; fully settled). This session: worked both review rounds on #202
(Codex P1 + 3 CodeRabbit findings, all fixed/replied/resolved), merged it,
released v0.1.3 locally, cleaned 57 branches + 31 worktrees, filed
#203-#207._

## Task Overview

Ship v0.1.3 publicly, clean. Blockers in order: fix #207, re-dogfood, tag
question, Ollie pushes the tag. Then #206 (dormant dimming) is the first
feature of the next cycle. Success = Ollie says the fleet feels right and
the public release is cut.

## Reference Docs

- Issue #207 — the blocker, with full constraint map and a suggested
  plugin-routed design. Start here.
- `crates/clave/src/setup.rs` ~L103-127 — the Alt+f `Run` bind and the
  comment recording the #188 trade you are now reversing.
- FOOTGUNS.md #188 entries (~L86-87) — NewPane drops its child block; a
  bind on a stock key REPLACES stock's action list per mode.
- FOOTGUNS.md L63-65 — starved bars get no Tab/PaneUpdates and self-claim
  active; executor-gate anything that acts (relevant if #207 goes
  plugin-routed).

## Current State

- main = origin/main = `1e2bffe` (the #202 squash), gates green, released
  locally as v0.1.3. **Tag v0.1.3 is cut locally, NOT pushed — do not push
  it; #207 blocks.**
- Dogfood so far: resume/recreate green (this very arc resumed across the
  fleet swap), Alt+c flurry green, id-reuse exercise green (zero
  `bind-evict` lines = prunes landed before ids were reissued — the healthy
  signature). Stable log rotated to `~/.local/state/clave/clave.log.pre-v013`;
  fresh log starts at v0.1.3.
- Issues filed: #203 (drive corroborates jsonl + first live pass of the
  now-concurrent burst), #204 (resume hydration from transcript jsonl),
  #205 (evlog single-write appends + rotation), #206 (dim dormant rows +
  terminal tab look), #207 (BLOCKER).
- Untracked in root, deliberate: `clave-working-sample-1.png` (old
  reference shot), the two newest `docs/status/` files.

## What's Working

- The `witnessed_dead` claim ledger in `crates/clave-bar/src/model.rs` is
  the pattern for prune/claim work: witnessed by frame diff, voided by
  rebirth in a frame, settled by the store echo, never consumed at emit.
  Its field doc carries the lifecycle.
- 186 bar tests + `just gates`; scoped `cargo mutants` (justfile recipe)
  for model.rs changes. The PR flow that just worked: push → CI → trigger
  CodeRabbit with `@coderabbitai full review` (plain `review` fails —
  reviews-on-request repo) → fix/reply per thread ("Ollie's Agent
  Speaking:") → resolve → merge.
- Alt+a / Alt+w / Alt+c are the user-facing verbs; Ollie never runs clave
  CLI commands — phrase everything as keybinds.

## Important Discoveries

- #207 is NOT an accident: #188 knowingly traded the toggle for a sized
  shell because `Run` is the only keybind form carrying floating geometry.
  The fix must keep the size AND restore the toggle. zellij-tile 0.44.3 has
  `open_command_pane_floating(+coordinates)` (shim.rs:624, `_near_plugin`
  :644) but NO `toggle_floating_panes` shim — hide/show needs the CLI
  `zellij action toggle-floating-panes` via subprocess or pane-id
  show/hide (FOOTGUNS 69/70: `show_self` focuses, `suppress_pane` damages
  swap state). Read the vendored source before building.
- zsh interactive mode does not honor `#` comments — any command block
  printed for Ollie must be comment-free or parens in comments glob-fail.
- The release gate counts untracked files as dirty (once bit by the
  reference PNG — hence "commit the screenshot tracked").
- GitHub had a major outage today; expect residual 503s — retry, don't
  redesign around them.

## Next Steps

1. **Fix #207, test-first, in a fresh worktree.** Gates + scoped mutants;
   PR; both bots' rounds worked and resolved. This is the only code
   blocker.
2. **Re-dogfood with Ollie** (Alt+f toggle open/hide/reopen, plus a quick
   Alt+c and add/close sanity), then ask him: move the v0.1.3 tag to the
   fix, or bump to v0.1.4 (Cargo.toml bump + changelog if so). Then print
   his comment-free block: retag/release/push-tag. The tag push cuts the
   public release via cargo-dist CI.
3. **Reference screenshot:** Ollie takes a fresh fleet shot post-fix. Commit
   it TRACKED in the repo root; ask whether the old
   `clave-working-sample-1.png` is replaced or kept alongside; update
   AGENTS.md to mandate looking at it before any session (directive
   already conversation-locked verbatim: "a new reference screenshot, that
   every future agent must look at before beginning a session to ground
   its understanding visually").
4. **Finish the cleanup he asked for:** 13 local review/push branches from
   the #190-#200 train are unproven squash-residue — get his one-word okay
   and bulk `-D` (`git branch | grep -E 'review|push-19|redesign-181|width-truth|fix-198|review200'`
   style, list first). Two dirty agent worktrees need his judgment:
   `agent-abec…` (staged model.rs edit, #181 era), `agent-ac2e…` (README
   edit, #188 era). KEEP `claude-codex-profile` worktree + branch (open
   #65, his explicit call).
5. **Then #206:** dim dormant rows' text/glyphs/labels vs live rows, and
   improve the terminal tab's look — ink/styling in
   `crates/clave-bar/src/render.rs` only, grounded in the new screenshot.
   His stated first feature of the next cycle. (#203/#204/#205 remain
   backlog, not blockers.)

## Context to Preserve

- His commands vs agent's: anything writing `~/.cargo/bin`,
  `~/.local/share/clave`, tags, releases, or touching his live zellij
  session is HIS — print comment-free blocks. Sandbox driving is the
  agent's, per-worktree instance only (`just sandbox`, ct.sh).
- Ollie is happy with the state and credits the design simplification —
  keep proposing the simple version first.
- A failed phase/anomaly is evidence to read, not a thing to retry until
  green. Grep FOOTGUNS before debugging anything odd.

## Restart Hint

Clean tree on main; safe to start immediately. Open with the #207 fix in a
fresh worktree — everything else queues behind it.

## Suggested Skills

- `superpowers:test-driven-development` — the #207 fix, red first.
- `superpowers:using-git-worktrees` — isolate the fix branch.
- `superpowers:systematic-debugging` — if the toggle design misbehaves live.
- `mattpocock-skills:writing-for-agents` — the AGENTS.md screenshot
  directive.
- `handoff` before stopping.
