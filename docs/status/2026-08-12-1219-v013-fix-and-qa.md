# Status — v0.1.3 held on #178; QA drive designed; fix first, QA build rides the same process

## Task Overview

Two threads, ONE process, priority ratified by Ollie at session end:
**first fix the live clave defects (#178 is the release blocker), and as part
of the same process build the QA testing system so it covers validating those
fixes.** The v0.1.3 cut is HELD: tag `v0.1.3` exists LOCALLY at `79771b5`,
unpushed — it stays local until #178 is fixed and re-validated (the fix will
re-tag). The QA system is designed (docs below) but unbuilt and UNRATIFIED —
**the fresh session opens with Ollie grilling the docs**
(mattpocock-skills:grilling against QA-DRIVE.md + the inventory) before any
build.

## Reference Docs

- @docs/status/2026-08-11-1830-v013-part-c-drive.md — the #178 discovery
  session: full evidence trail, release-tail state, next-session plan for the
  fix. Read it in full (short).
- `docs/dev/QA-DRIVE.md` (whole file, ~90 lines, DRAFT) — the 7-phase drive
  design under grill.
- `docs/dev/qa/BREAKAGE-INVENTORY.md` — index of 99 breakage classes +
  corrections + known-liar detectors; per-item specs in
  `docs/dev/qa/inventory/*.md` (7 plane files; open only the plane you need).
- `docs/dev/qa/inventory/pipe-delivery.md` § P9 — the #178 spec.
- `docs/dev/TESTING.md` §"The sandbox drive loop" (~L740) + RELEASE-RUNBOOK.md
  Part B/C — the fix's validation SOP and the resumed cut.

## Current State

All work UNCOMMITTED (untracked only, working tree otherwise clean, main at
`79771b5` = merged bump PR #177):

- `docs/dev/QA-DRIVE.md` — draft design: agent-driven `scripts/qa-drive.sh`
  via ct.sh; phases: preflight → baseline join → **wake ladder** → tab churn
  → ring walk → collapse burst → quiescence → teardown; tracing spec (one
  drive log, log-marked reads, exact EOF-twin accounting); needs new
  `qa-fleet` scenario (6 dormant rows: one worktree, one stale-cwd, one
  rotated live_session, three plain).
- `docs/dev/qa/` — the inventory (index + 7 plane files, 1172 lines,
  anchors verified 2026-08-12, PII-clean).
- `docs/status/2026-08-11-1830-v013-part-c-drive.md` — prior handoff,
  untracked on purpose.
- GitHub: issues #178 (bind regression, OPEN, blocker) and #179 (single-ring
  nav simplification, post-fix scope) filed this cycle; map #115 closed;
  milestone closed; Part A baseline commented on #115.

## What's Working

- **The v0.1.3 artifact set is coherent and live-validated except #178**:
  Part C passed formally (one sidebar per tab, `build=v0.1.3` on every load,
  stable+launch KDL at one version, unversioned probes empty, doctor exit=0).
  Only the wake-bind defect blocks the go. Ollie's daily fleet runs on it —
  usable; affected tabs are real claude sessions merely mislabeled as
  terminal tabs.
- **The #178 evidence is solid and re-derivable from the prior handoff**:
  CLI leg healthy (evlog `open`/`spawn` lines), hooks healthy (status flips,
  transcripts append), registers all delivered (EOF-twin arithmetic 1+2+3+4),
  loss is the bar's bind leg; deterministic across retries; regression window
  = PR #120 (`bind_sent` budget + `frames_coherent` gating — merged AFTER
  v0.1.2) and #162/PR #171 adjacent. `is_dormant`'s `pane_live` suppression
  (model.rs) is what makes the row vanish per-instance when a bind never
  lands.
- **The inventory is verified**: file:line anchors re-checked against current
  source (FOOTGUNS' had drifted); corrections recorded in the index (K2 guard
  overstated/#114 open; S9 structurally closed by `Store::mint_ord`; K3
  conditional on #114; B14 semi-automatable via store recency probe;
  `tab_timeline`→`tab_order`; twins prove DELIVERY never failure).
- **Working session patterns**: evidence reads of Ollie's store/logs are
  sanctioned and productive (`~/.local/state/clave/agents.json` + `clave.log`,
  zellij log EOF-twins); the CodeRabbit protocol ran twice cleanly this cycle
  (fix → reply "Ollie's Agent Speaking:" → resolve, GraphQL
  addPullRequestReviewThreadReply + resolveReviewThread).

## Important Discoveries

- Codex review caught the README still saying `git checkout v0.1.2` on the
  bump PR — the versioned checkout line must ride every future bump.
- `$TMPDIR` scratch files do not survive to the post-cold-start shell — the
  runbook Step 0 checks file must be re-created in the live session's shell
  (bit Ollie during Part C; consider a runbook amendment with the fix PR).
- Waking a remote-controlled conversation claims it locally (`--resume`
  side-effect) — R4 in the inventory, unguarded, noted only.
- The evlog interleaves concurrent writers (garbled JSON lines are cosmetic).
- Failed attempts to avoid repeating: retrying the wake does NOT heal #178
  (two deterministic failures); reading `dropped … empty payload` lines as
  failure evidence is the #162 misdiagnosis — they are delivery proof.

## Next Steps

1. **Grill first**: Ollie grills QA-DRIVE.md + inventory (his opening move,
   his words below). Expect scope cuts; the design's own descopes (no CI, no
   screenshots, no auto-bisect) were deliberate — defend or concede openly.
2. Settle his open review questions: phase spine OK? `qa-fleet` OK? phases
   0–2 as PR 1 OK? runbook Part A gains "qa-drive green"?
3. **Fix #178**: sandbox repro via the wake-ladder shape (P9 spec has the
   steps) against v0.1.3 wasm, then v0.1.2 wasm to pin the window; read
   SUBSYSTEM-VALIDATION S0/C5 sections + FOOTGUNS TabUpdate-starvation notes
   BEFORE instrumenting; fix at root cause; gates + CodeRabbit protocol.
4. Build the QA system as part of the same process (PR 1 = `qa-fleet`
   scenario + qa-drive.sh phases 0–2 — phase 2 IS the #178 harness); commit
   the qa docs + both handoffs with it.
5. Resume the cut: Ollie deletes + re-tags on the fix commit, `just release`,
   Part C again, push on go; then Part D (#115 comment, runbook tail).
   #179 stays post-fix.

Stopping exchange, verbatim (Ollie): "Make sure that the handoff is correctly
written to pick all of this back up. We first need to fix the issues that we
are seeing in Clave itself and then, as part of the same process, we're going
to build out the testing system so that we can cover the testing of this
implementation and make sure that QA and the fixes are good across Clave.
/handoff - When we get back in the fresh context window, we will start with a
grill with docs."

Endorsement of the design direction, verbatim (Ollie, on the inventory):
"Great, I believe there needs to be more context on the breakage inventory
for each item - consider what would need to be there independently per item
for an engineer to build the flow to test it correctly, without it fumbling
for meaning." — that expansion is DONE (the 7 plane files).

## Context to Preserve

- **His zellij session: nothing, not even a read** via zellij actions; store
  and log READS are fine. Sandbox only through `scripts/ct.sh` (env-var route
  fails OPEN). Launch/kill/`just release`/`~/.local/share/clave` writes are
  his. Merges are his (permission classifier blocks `gh pr merge`).
- Commits author+signature = Ollie; `grep -c "/Users/"` any body before
  posting (public repo); "Ollie's Agent Speaking:" on anything humans read
  as him; CodeRabbit threads: never silent-resolve.
- KISS bias (memory kiss-over-engineered-plans): propose the simple version,
  descope openly; six-sentence reports, decision not mechanism.
- Merge chains: sleep 90 before `gh pr checks --watch`; merges from repo
  root; "base branch policy prohibits" = unresolved threads.

## Restart Hint

`/catch-up` lands here. Nothing to stash (untracked docs only — leave them
untracked so /catch-up finds the handoffs; they commit with QA PR 1). Open
with the grill; touch no code before #178's sandbox repro.
