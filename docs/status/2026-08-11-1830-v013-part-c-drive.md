# Status — v0.1.3: Part C found a bind regression; cut HELD, tag local

_2026-08-11 ~19:30 · supersedes the 15:00 release-tail handoff (fully
resolved: #176 merged, map #115 closed + Part A baseline commented,
milestone closed, bump PR #177 merged at `79771b5`). This file is the
truth for the next session._

## Where this stands

- **Tag `v0.1.3` exists LOCALLY at `79771b5`, unpushed — and must stay so.**
  On the fix it gets deleted and re-cut (`git tag -d v0.1.3`, his command).
- `just release` ran; formal Part C checks all PASSED (one sidebar,
  `build=v0.1.3` on every load, stable + launch.kdl coherent at one version,
  unversioned probes empty, doctor exit=0).
- **Step-4-class NO-GO: issue #178.** Wakes beyond the session's early tabs
  never bind: tab renders as a terminal row, dormant row stays suppressed
  per-instance (`is_dormant` `pane_live` suppression turned permanent).
  Deterministic across retries. CLI leg (open/spawn/resume), hooks, and
  register broadcasts all proven healthy from store + clave.log + zellij-log
  EOF-twin counts; the loss is the bar's bind leg. Regression window: PR
  #120 (bind_sent budget + frames_coherent gating) merged AFTER v0.1.2 —
  first daily-driver exposure was this session; #162 (PR #171) also touched
  that neighborhood.
- Ollie's fleet is USABLE meanwhile: the conversations in the "terminal"
  tabs are real resumed claude sessions (hooks fire, transcripts append) —
  only the bar's classification is wrong. Side-finding: waking a
  remote-controlled conversation claims it locally (`--resume` behaviour).
- **#179 filed**: Ollie's direction to remove the two-block nav ring — one
  ring over one ordered list. Post-fix scope, NOT part of #178.

## Next session, in order

1. **Reproduce #178 in the sandbox** (TESTING.md drive loop, ct.sh only):
   scenario with ≥4 sequential wakes; instrument bind_effects emission vs
   apply_bind arrival vs prune traffic; confirm against v0.1.3 wasm, then
   v0.1.2 wasm to pin the window. Read SUBSYSTEM-VALIDATION C-sections for
   S0 first; FOOTGUNS on TabUpdate starvation of hidden instances.
2. Fix at root cause, PR through gates + CodeRabbit protocol (fix, reply,
   resolve every thread; merges are Ollie's — classifier blocks
   `gh pr merge`).
3. Re-cut: Ollie deletes + re-tags on the new commit, `just release`,
   Part C again (checks file must be REWRITTEN in the live shell — $TMPDIR
   copies don't survive; see runbook Step 0), then push tag on go.
4. Part D: comment Part C results on #115, commit this handoff.

## Standing constraints

His session untouchable (evidence reads of store/logs are fine; zellij
actions are not). Commits Ollie-signed. `grep -c "/Users/"` before posting.
"Ollie's Agent Speaking:" attribution. Evlog interleaves concurrent writes
(garbled JSON lines are cosmetic, not corruption).
